# {{ app_label }}

{{ plan.app_description }}

Generated by Heron.

## Data source

- Sourcetype: `{{ plan.data_source.sourcetype }}`
- Input path: `{{ plan.data_source.input_path }}`
- Parsing strategy: {{ plan.data_source.parsing_strategy }}

### Fields extracted

{% for field in plan.data_source.fields_extracted %}
- `{{ field.name }}` ({{ field.type }}) — `{{ field.extraction }}`
{% endfor %}

## Saved searches

{% for search in plan.saved_searches %}
### {{ search.name }}

{{ search.description }}

Schedule: every {{ search.schedule }}

```
{{ search.spl }}
```

{% endfor %}

## Dashboards

{% for dashboard in plan.dashboards %}
- **{{ dashboard.name }}** ({{ dashboard.panels | length }} panels)
{% for panel in dashboard.panels %}
  - {{ panel.title }} (`{{ panel.viz_type }}`, from `{{ panel.search_name }}`)
{% endfor %}
{% endfor %}

## Alerts

{% for alert in plan.alerts %}
### {{ alert.name }}

- Severity: {{ alert.severity }}
- Condition: {{ alert.threshold_condition }}
- Action: {{ alert.action }}
- Based on: `{{ alert.search_name }}`
{% endfor %}
{% if plan.notes %}

## Notes

{{ plan.notes }}
{% endif %}
